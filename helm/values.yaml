# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "mikityukvarvara/kbot"
  tag: "v"
  os: linux
  arch: ""
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
